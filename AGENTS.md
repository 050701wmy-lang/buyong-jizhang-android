# Accounting 项目约束

## 项目定位

1. 本项目是原生 Android 个人记账应用，不适用任何 Fabric 或 Minecraft 项目约束。
2. 使用 Kotlin、Jetpack Compose、Navigation 3、Room 与 MIUIX Compose Multiplatform 组件库。
3. 数据以本地 Room 数据库为单一事实来源，默认离线可用，不要求登录。
4. 所有记账草稿与批量写入必须先经用户确认后才允许落库。
5. 金额使用最小货币单位的 `Long` 存储，不得使用 `Float` 或 `Double` 保存金额。

## 开发规则

1. 写 API 前先查当前版本官方文档与 MIUIX 源码，不猜测签名。
2. 禁用已弃用或旧 API，不把旧 API 当作替代方案。
3. 坚持 KISS，保持执行流程平铺、调用栈浅，不做无需求的抽象和模块化。
4. 手动记账、导入与周期任务必须汇入同一条交易保存流程。
5. 重复逻辑抽到共用函数或父层，不在页面或子类中复制。
6. 默认信任已经由固定入口构造完成的状态，不分散添加兜底和防御性判断。
7. 不修改已有注释；不得把本文件内容复制到代码注释中。
8. 每个类与函数都要在定义前写说明用途的中文 KDoc。
9. 注释描述当前逻辑，不记录“本次修改了什么”。
10. 字符串资源名、数据库列名、JSON 字段名和其他字符串标识统一使用 `snake_case`。
11. 不写完整类包名，使用 `import`。
12. 修复问题时定位根因，只改指定问题，不顺带修复其他潜在问题。

# UI 规范

## HyperOS 视觉语言

1. 整体视觉保持轻、净、连续；层级主要通过留白、浅色 surface、squircle、透明度和 Blur 建立，不依赖粗描边、强阴影或高对比色块。
2. 普通页面以中性浅色为主，主要内容使用高对比文字，说明文字、尾部值和不可用状态依次降低对比度，不为次要信息抢占主色。
3. 字号、字重和行高统一从 MIUIX Typography 取得；主标题、分区标题、正文和说明形成稳定层级，不在页面内临时拼接相近但不一致的文字样式。
4. 内容按语义分组，组内行保持连续白色或 surface 背景，组间使用浅灰背景与留白分隔；普通列表不为每行增加独立边框或悬浮阴影。
5. Preference 行统一使用“左侧彩色 squircle 图标或文字区、中央主副文案、右侧值/开关/箭头”的结构，同组行的基线、图标尺寸和尾部控件位置保持一致。
6. Preference 与同级选择行的行尾箭头统一使用 MIUIX `MiuixIcons.Basic.ArrowRight`，尺寸为 `10.dp × 16.dp`，颜色使用 `onSurfaceVariantActions`，不得用说明文字色或其他 Chevron 近似替代；尾部文字、图标与箭头放入固定高度 Row 垂直居中，品牌或位图图标存在光学偏移时统一向上修正 `1.dp`；顶栏下拉选择使用同一图标顺时针旋转 `90°` 明确指向下方。
7. 主题主色只用于主操作、选中项、活动开关、进度和必要强调；大面积背景与普通图标保持中性，避免同一页面出现多个竞争强调色。
8. 渐变只用于明确的品牌区、插图或需要突出数据的 hero 区域，普通 Card、表单和列表保持平面纯色，不把渐变当作通用装饰。
9. 异步数据、金额和计数更新时预留稳定布局，内容变化不得造成 Card 高度、尾部控件或相邻列表项无意义跳动。
10. 页面使用 edge-to-edge，状态栏与导航栏的明暗、背景和图标对比度跟随当前 surface；系统栏过渡不得先于或滞后于页面内容闪变。

## 动效总则

1. 动效以空间连续性和直接操控感为核心；进入、展开或创建动作从用户刚刚操作的元素或方向开始，避免无来源的全屏淡入。
2. 优先使用 MIUIX 组件与 Navigation 3 已提供的动效；确需自定义时先查当前版本文档与源码，复用同一套 motion spec，不为单个页面随意定义时长和缓动。
3. 一次交互只保留一个主运动关系；位移、缩放、圆角、阴影和透明度围绕同一来源同步变化，不让多个无关元素同时抢动画焦点。
4. 几何变化负责表达层级和方向，透明度只用于辅助遮罩与内容显隐；前进、返回和层级切换不得仅用全屏淡入淡出代替空间关系。
5. 进入与退出必须可逆，起点、终点和圆角状态相互对应；动画中断、快速返回或手势取消时应从当前进度平滑收回，不跳到预设端点。
6. 弹簧只用于位移、尺寸或圆角变化的收尾，并保持克制；禁止重复弹跳、装饰性抖动以及不同组件各自使用不一致的弹性。
7. 动画期间内容始终裁剪在当前 squircle 或容器轮廓内，涟漪、背景、阴影与内容共同运动，不出现方角闪帧或越界绘制。
8. 数据刷新只动画真正变化的数字、进度或列表项，不重播整页入场；同一状态变化不得同时叠加组件内置动画和页面自定义动画。
9. 遵循系统动画缩放与减少动态效果设置；减少动效时保留状态反馈，但缩短或移除大幅位移、缩放和弹簧，不影响操作完成。
10. 自定义动效必须以慢速或逐帧检查开始、结束、反向和取消四种状态，确认无白屏、背景闪烁、标题重影、布局跳变和重复导航。

## 页面转场与滚动

1. 手机端进入二级页面时，目标页从右侧以带大圆角的完整 surface 进入并逐步铺满，来源页轻微向左退让并降低亮度；不得只让零散控件分别飞入。
2. 返回动效与前进动效严格反向；边缘返回与 predictive back 必须随手势进度移动，取消时平滑回到原页面，完成时再提交 Navigation 3 栈变化。
3. 连续点击入口不得重复压入相同 Navigation 3 entry；转场期间保持单一导航结果，不因动画未结束产生双页或状态重复提交。
4. 从悬浮按钮、图标或明确容器进入创建/编辑页时，优先使用从来源容器扩展的转场，位置、尺寸和圆角连续变化；没有可靠共享来源时使用统一页面转场，不伪造共享元素。
5. 手机端首页、明细和报表主 Tab 使用 Compose Foundation 水平 Pager 支持左右手势切换，顶栏标题、顶栏操作与底部导航选中态跟随 Pager `currentPage` 在越过页面中点时立即切换，不得等待落页动画完全停止，并保留各 Tab 的滚动位置和页面状态；从属于单个 Tab 的悬浮按钮必须跟随所属页面实时水平移入或移出，不得等落页后突然显隐；设置页不参与 Pager，宽屏保持侧栏直接切换；主 Tab 切换不得使用二级页的整页入栈滑动。
6. 手机大标题随列表滚动连续收敛为居中小标题，返回时连续展开；大小标题表示同一语义状态，不允许两份标题交叉重叠、瞬间替换或与列表各自滚动。
7. 滚动、fling 与 overscroll 统一交给 MIUIX 和 Compose 的嵌套滚动链处理，保持轻微弹性反馈；不得再叠加手写位移造成双重回弹。
8. 宽屏转场只作用于实际内容区，左侧导航栏保持稳定；不得把侧栏一起推离屏幕，也不得按包含侧栏的整窗宽度计算转场距离。
9. 应用启动、退回桌面和最近任务卡片的缩放、壁纸模糊与任务圆角由系统负责，应用内不得复制 launcher 级动画或截取桌面作为背景。
10. 顶栏、底栏、状态栏和导航栏与内容使用同一转场进度；目标页未覆盖到的区域保留来源 surface，不出现独立变黑、变白或提前切色。

## 弹层与状态变化

1. Dialog、WindowListPopup 与底部弹层出现时使用统一半透明遮罩压暗来源页，来源内容保持可辨识但停止交互；遮罩和弹层必须同步进入与退出。
2. 就地选择弹层从触发行附近以缩放加淡入展开，保持 squircle 轮廓；选项内容使用 `20.dp` 水平内边距与 `16.dp` 垂直内边距，主副文案左对齐，确认图标固定在尾部；选中项使用主题主色和确认图标，说明文字保持次级对比度。
3. 底部弹层从屏幕底部整体上移并保留明显的顶部大 squircle，标题、选项和按钮随容器一起运动，不让内部内容先于容器浮现。
4. 弹层关闭使用出现动效的严格反向；点击遮罩、取消、确认和返回键都先收起同一个弹层，再恢复来源页亮度，不直接移除遮罩或内容。
5. 弹层出现与消失不得触发来源列表重新布局、滚动复位或重新加载；确认选择后只更新触发行和关联内容。
6. Switch、分段栏、单选项和进度条的轨道、滑块、颜色与文字状态同步过渡，状态值只提交一次，不以多段延迟动画模拟响应。
7. 悬浮按钮按下时使用 MIUIX 内置涟漪与轻微压缩反馈；若其打开创建页，按钮反馈结束后接续容器或页面转场，不同时播放互相冲突的缩放。
8. 插入、删除、展开和收起只动画受影响的列表项及相邻位移，静态分区、整组 Card 和固定栏保持稳定。
9. 底部弹层标题栏右上角操作按钮对齐 MIUIX `WindowBottomSheet` 的 `endAction` 基准：使用 `35.dp` 最小点击区与 `24.dp` 图标，相邻按钮保持 `5.dp` 间距，按钮组距离弹层右侧 `20.dp`，不得贴边、下沉或挤压居中标题。
10. 底部弹层内的 Card 或卡片网格距离弹层左右边缘统一为 `20.dp`；卡片自身间距仍按所属页面规范处理，不用增大弹层整体 `insideMargin` 影响标题栏与其他内容。

## MIUIX 组件

1. 所有可见 UI 优先使用 MIUIX 组件，包括但不限于 `Card`、`Scaffold`、`TopAppBar`、`NavigationBar`、`TextButton`、`TextField`、Dialog 与 Preference。
2. MIUIX 组件内部已经处理的 squircle、阴影和涟漪不得重复实现。
3. MIUIX 没有对应组件时才允许使用 Compose Foundation 布局或绘制能力，并保持 MIUIX 主题颜色、字号和动效。
4. 返回按钮使用 MIUIX 返回图标；主导航图标使用 MIUIX Icons。
5. 操作型图标按钮的最小宽高为 `35.dp`，背景使用 `secondaryContainer`；所有页面的顶栏右上角操作按钮统一使用 `35.dp` 最小点击区、`24.dp` 图标、相邻按钮 `5.dp` 间距、`20.dp` 右侧留白与透明背景，不得为单个页面另设尺寸或位置。
6. Badge 使用 `3.dp` squircle 与 `9.sp` 粗体等宽字体。

## Squircle

1. 自定义形状统一使用 `top.yukonga.miuix.kmp.squircle` 提供的 API。
2. 禁止用 `RoundedCornerShape` 模拟 squircle。
3. 非点击纯色背景使用 squircle background，不额外 `clip`，避免多余离屏层。
4. 图片和必须裁剪的内容使用 squircle clip。
5. 可点击元素先填充/裁剪到 squircle，再添加点击处理，保证涟漪在圆角范围内。
6. `3.dp` 小圆角可直接使用普通圆角，不需要 squircle。

## 页面骨架

1. 页面使用 MIUIX `Scaffold`、`TopAppBar` 与 `LazyColumn`；所有手机页面必须显示左对齐大标题并连接内容滚动，滚动后连续收敛为居中小标题，只有宽屏页面使用固定 `SmallTopAppBar`。
2. `LazyColumn` 必须连接 TopAppBar 的滚动行为与 overscroll。
3. 列表只使用 Scaffold `innerPadding` 的 top padding；bottom padding 由统一尾部 item 处理。
4. 第一个列表 item 先放 `12.dp` 间距，再开始内容。
5. 最后一个列表 item 统一包含 `24.dp` 间距、导航栏 inset 和底部留白。
6. 二级页面使用独立 Navigation 3 entry，不显示底部主导航。
7. 二级页面的底部安全区由内容尾部 `navigationBarsPadding()` 处理，不把底部 inset 重复传给列表。
8. 首页、明细和报表三个主 Tab 的内容由外层主 Scaffold 承担底部导航，页面必须接收并使用外层 bottom padding；设置通过顶栏右侧图标进入。

## 顶栏与底栏

1. 所有页面的顶栏与底栏使用 MIUIX Blur 能力包裹。
2. Blur 可用时栏背景透明；Blur 不可用时使用 `MiuixTheme.colorScheme.surface`。
3. 内容列表必须提供给 Blur 所需的内容层，不允许顶栏与底栏各自抓取互不一致的内容。
4. 含动态搜索框的页面，搜索状态切换不得出现不透明色块遮挡毛玻璃。

## 宽屏与横屏

1. 窗口宽度达到 `600.dp` 时视为宽屏。
2. 手机使用底部 `NavigationBar`；宽屏改用左侧可展开/收起的导航栏。
3. 导航栏收起时仍需避开 cutout 与系统栏；底部安全区传递给各主 Tab。
4. 宽屏导航的首页图标与手机底栏首页图标保持一致。
5. 宽屏页面使用固定不折叠的小标题 TopAppBar；手机使用可折叠的大标题 TopAppBar。
6. 搜索页在宽屏仍保留动态搜索 TopAppBar，但 TopAppBar 固定不折叠。
7. 主 Tab 的内容用宽屏内容容器居中，内容最大宽度为 `800.dp`。
8. 内容宽度应按实际内容区计算，不把侧栏宽度重复计入。
9. 横屏时 MIUIX Scaffold 只提供系统 inset，不会自动避开刘海、挖孔与手势条；二级页内容必须补齐横向 cutout padding。
10. 顶栏自身已处理的 inset 不得重复添加；只有明确关闭默认 inset 的顶栏才由页面补齐横向 padding。

## Card 与列表

1. Card 水平间距统一为 `12.dp`。
2. 每组 Card 使用 `Modifier.padding(horizontal = 12.dp).padding(bottom = 12.dp)`。
3. 禁止在 `LazyColumn` 中为每一行单独放置互相分离的 Card。
4. 多行内容使用 grouped card items，首行、末行和中间行分别具有连续的 squircle 外轮廓；所有带组头和内容行的列表型 grouped Card 统一使用 `40.dp` 组头最小高度、`16.dp` 水平内容内边距、`12.dp` 内容行垂直内边距、`40.dp` 图标容器和 `24.dp` 图标，单条内容时形成约 `104.dp` 的基准总高度，多行文字和大字体场景允许自然增高。
5. grouped card 的分隔背景必须裁剪，防止点击涟漪从方角溢出。
6. 可滚动的 grouped card item 使用 `animateItem()`；纯静态提示卡不添加列表动画。
7. 展开与收起时只给真正变化的列表 item 添加动画，避免整组同时跳动。
8. Card 不固定为屏幕高度；长内容允许自然增长，短内容通过合理的最小高度保证视觉稳定。
9. 列表型空状态 Card 统一复用共用组件，使用 `12.dp` 水平外边距、`12.dp` 底部间距、`18.dp` 垂直内容内边距与次级色居中文案；不得在单个页面另写更高或左对齐的空状态 Card。
10. 首页资产、明细摘要和账户详细页余额等顶部数据 Hero Card 统一使用 `21:9` 宽高比；同构 Hero 使用一致的内容内边距与 `SpaceBetween` 垂直骨架，标题、主金额和底部统计行必须保持相同纵向位置。

## 表单与 Dialog

1. `TextField` 表单不放进 Card，直接使用 `padding(horizontal = 12.dp).padding(bottom = 12.dp)`。
2. 编辑 Dialog 的按钮顺序为：未修改、取消、确认；确认按钮使用主题主色文字。
3. 长内容使用 MIUIX Window Dialog，手机端内容 Column 不设置固定最大高度，让内部内容适配屏幕。
4. 所有输入先更新页面状态，确认时再通过统一交易保存流程提交。
5. 两到八个固定单选项优先使用 MIUIX WindowListPopup 就地选择，显示背景压暗、当前项主色和确认图标；有必要时可在选项下方显示说明。
6. 只有选项需要搜索、创建、管理或承载复杂状态时才进入独立二级选择页，不为固定枚举选项增加页面跳转。

## 手动记账页

1. 手动记账是独立二级页，标题使用“记一笔”，左侧使用关闭图标，不显示主导航。
2. 页面依次展示固定居中小标题、左对齐连续排列的支出/收入类型栏、金额、真实分类宫格、时间和备注，底部固定通栏保存按钮；不显示“其他”类型，点击金额后才显示应用内数字键盘。
3. 收支类型和分类只能来自现有数据模型与 Room 数据，不为匹配参考图伪造“其他”类型或额外分类。
4. 金额键盘支持数字、小数、删除和一次加减运算；金额仍以最小货币单位 `Long` 提交。
5. 分类宫格每行最多五项，使用 MIUIX 图标、主题色和 squircle 选中状态。
6. 未选择的账户沿用当前默认账户；确认时仍汇入统一交易保存流程，不复制新的写入逻辑。
7. 参考图优先约束手动记账页内容视觉；MIUIX 默认表单外观不匹配时允许使用 MIUIX 主题配合 Compose Foundation 组合。
8. 数字键盘显示时在系统导航栏 inset 之外保留额外底部安全距离，避免最后一排按键贴近圆角屏幕边缘。
9. 页面不显示账户选择行，新建时沿用预选账户或当前默认账户，编辑时沿用原账户。
10. 分类区使用每行五项的图标宫格、squircle 容器与底部分页指示；时间和备注使用无 Card 的连续行，日期编辑使用带遮罩的 MIUIX WindowBottomSheet，在日期三列滚轮与时刻两列滚轮之间切换，滚轮选中值统一使用 `28.sp` 中等字重与主题蓝色。
11. 备注输入时允许系统键盘覆盖底部保存操作，系统键盘收起后保存操作必须立即恢复；点击金额必须清除备注焦点并重新打开应用内数字键盘，点击金额区外的页面空白、类型、分类或时间区域必须收起应用内数字键盘。

## 统计页

1. 统计页标题使用“收支报表”，底部主导航标签保持“报表”。
2. 顶部依次提供周报、月报、年报周期切换，上一周期/下一周期与当前日期范围，以及支出/收入切换。
3. 周期摘要使用一个双列 Card，显示周期总额、日均金额、相比上一周期的变化和周期收支结余。
4. 摘要之后依次放置当前周期趋势、最近六个周期趋势和分类构成三个 Card。
5. 趋势图与分类构成必须使用真实账目数据；没有数据时显示“暂无记录”，不得伪造演示数据。
6. 周报按日聚合、月报按周聚合、年报按月聚合；最近周期趋势固定展示包含当前周期在内的六个周期。
7. 分类构成按金额降序排列，并同时显示金额、占比和 MIUIX squircle 进度条。
8. 统计内容沿用主页面的 `12.dp` Card 间距、`800.dp` 宽屏上限和统一列表尾部安全区。
9. 统计参考图只约束报表分页内容，不改变统一大标题、主顶栏、三项底部导航与悬浮记账按钮。
10. 报表分页内容以参考图的视觉层级为优先；MIUIX 默认组件外观不匹配时，允许使用 MIUIX 主题色、图标与 Compose Foundation 组合实现。
11. 周报、月报、年报使用一个无独立描边的整体分段栏；日期区间与支出、收入切换保持单行紧凑布局。
12. 摘要、趋势与分类 Card 显式使用 `16.dp` 内容内边距；无数据状态使用浅蓝背景的纸飞机图形和“暂无记录~”。

## 明细页

1. 明细参考图只约束“明细”主分页内容，不改变统一大标题、主顶栏、三项底部导航与悬浮记账按钮。
2. 不实现“微信支付宝账单自动记”横幅，不显示第三方自动记账来源标签。
3. 顶部摘要展示今日支出、本月支出和本月收入，金额全部来自 Room 中的真实账目。
4. 账目按设备时区的自然日倒序分组，每个日期使用一个 Card，并在标题行显示当日支出与收入小计。
5. 账目行展示分类图标、对象或分类名称、时间、备注或账户信息以及带正负号的金额。
6. 分类图标映射与手动记账页共用，不复制分类判断逻辑。

## 首页

1. 首页参考图只约束首页分页内容，不改变统一大标题、主顶栏、三项底部导航与悬浮记账按钮。
2. 首页不显示“快速记账”和最近账目，记账入口由统一悬浮按钮承担，账目列表由明细页承担。
3. 顶部资产 Card 展示净资产、总资产和总负债，全部由 Room 中真实账户余额计算。
4. 账户余额等于期初余额加收入减支出；正余额计入总资产，负余额绝对值计入总负债。
5. 账户按账户类型分组，只显示真实存在的账户与非空分组，不伪造参考图中的账户。
6. 每个账户分组使用一个连续 Card，标题行显示分组小计，账户行显示类型图标、名称和实时余额。
7. 设置入口固定在主顶栏右上角，不占用底部主导航位置。
8. 首页和报表不显示右下角悬浮按钮；只有明细页保留进入“记一笔”的浅蓝色圆形加号，手机端尺寸为 `56.dp`，相对 MIUIX 默认右下角位置向左偏移 `22.5.dp`、向上偏移 `21.5.dp`。
9. 新增账户入口固定在首页顶栏右上角并位于设置按钮左侧，使用统一顶栏操作按钮尺寸与透明背景；其他主分页不显示新增账户入口。
10. 首页账户行本身承担账户编辑入口，不从设置页增加独立账户管理入口。
11. 点击首页账户先进入账户详情页；账户详情页右下角加号进入“记一笔”并预选当前账户，右上角进入账户编辑页；手机端圆形加号尺寸为 `56.dp`，相对 MIUIX 默认右下角位置向左偏移 `22.5.dp`、向上偏移 `21.5.dp`，宽屏不使用该手机偏移。
12. 当前账户路由必须显式传递 `account_id`，为后续多账本增加 `ledger_id` 保留清晰扩展位置，但本阶段不预建多账本数据结构。
13. 首页账户分组标题行必须支持展开与收起；展开状态显示向上箭头并展示账户行，收起状态显示向下箭头并只保留分组标题和小计。
14. 首页账本 Hero Card 静止时统一保持 `21:9` 比例，并在前景卡上方最多露出两层轻微缩小、无清晰文字的真实账本背景卡片作为纵深提示。切换时动画对象是完整账本 Card：外壳、圆角、阴影、封面和文字沿同一手势进度共同换层，禁止固定外壳而只切换内部内容，也禁止整张卡跨越自身高度进行硬覆盖。入场卡从手势反方向以略小尺寸、低透明度和不超过卡片高度 `6%` 的位移进入前景；当前卡顺手势方向小幅位移、轻微放大并淡出到背景。向下拖动切到前一账本时，新卡从上方进入、旧卡向下退层；向上拖动严格镜像。背景账本身份也必须连续接力：当前远层在切换中逐步升为下一状态的近层，新的远层从透明状态预热出现，不得在主卡落位后重新计算并突然显现。手势进度、来源和目标账本必须在一次过渡中保持一致，快速连续拖动、中断和取消均从当前进度连续接管；越过阈值后柔和吸附并一次提交目标账本，未越过则平滑回到原账本。不得提供长按展开、瀑布流或其他账本选择交互。

## 账目与分类编辑

1. 新建账目与编辑账目复用同一记账编辑器；新建标题为“记一笔”，编辑标题为“编辑”。
2. 点击明细账目直接进入编辑模式，不额外建立只读详情页。
3. 编辑模式预填原账目的收支、金额、账户、分类、时间和备注，底部显示删除与保存。
4. 删除账目前必须使用 MIUIX Dialog 二次确认，保存时更新原记录，不创建重复账目。
5. 分类宫格末尾固定提供“添加”入口，使用 MIUIX Window Dialog 输入名称并选择持久化图标。
6. 新分类方向跟随当前收支类型，写入 Room 成功后立即选中；同方向分类名称不得重复。
7. 已被历史账目引用的账户与分类通过停用隐藏，保留原记录与外键关系。

## 账户编辑页

1. 账户编辑参考图的页面结构与视觉层级优先于通用 Card 规则，使用 MIUIX 不代表把每个字段包进 Card。
2. 顶部使用透明背景的小标题栏，仅放返回与确认图标；页面内容首项显示“添加账户”或“编辑账户”大标题。
3. “基本信息”分区只将账户名称作为独立 TextField；账户类型、图标、币种和余额使用无 Card 的 Preference 行。
4. 账户类型通过独立二级选择页修改，选择页展示持久化类型的图标、名称和可选说明；右上角提供自定义类型入口，类型名称必填、类型说明选填；余额通过 MIUIX Window Dialog 修改，不在页面内使用三段按钮或并排选择块。
5. 账户图标使用独立持久化字段，可从内置图标二级页选择或通过右上角入口选择自定义图片；币种只读显示人民币，不伪造可保存选项。
6. “账户设置”使用 BasicComponent 与 SwitchPreference；单账本阶段“适用账本”只读显示“全部”。
7. 底部固定显示删除与保存按钮，并使用 MIUIX Blur；删除前必须二次确认，只允许删除未被历史账目引用的账户。
8. 账户类型选择页预置现金、储蓄卡、信用账户、网络账户、投资账户、储值卡和虚拟账户七类持久化类型，并与用户自定义类型显示在同一连续 Card 中；当前项使用主题主色和确认图标。
9. 账户编辑页的账户类型行尾使用“类型名称 + 统一箭头”，账户图标行尾使用“图标 + 统一箭头”且不重复显示图标名称；两行尾部内容均按统一固定高度垂直居中，并遵循 Preference 行尾箭头颜色与光学校正规则。
10. 账户编辑页存在未保存的字段或适用账本更改时，通过顶栏返回、系统返回或边缘返回离开均必须先显示“放弃编辑”确认 Dialog；取消继续编辑，确认放弃后才退出页面。
