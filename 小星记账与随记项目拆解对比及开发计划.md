# 小星记账 4.0.8 与「随记」项目拆解、差距分析及开发计划

## 1. 结论先行

`小星记账 4.0.8` 已经是一个成熟的个人财务产品，而 `suiji-android` 是一个结构清楚、界面方向明确、核心记账闭环已初步成立的 MVP。两者最大的差别不是页面数量，而是账务模型的完备度、数据安全与迁移能力、复杂操作入口以及长期使用能力。

截至 **2026-08-02**，随记已经完成首页资产与账户列表、账户详情、账户编辑、收支 CRUD、分类新增、基础报表和本地 AI 草稿解析。当前阶段不应继续横向增加页面，而应把“能演示”推进到“可以安全承载真实账本”。

最值得借鉴的顺序是：

1. 先补牢账务内核：可编辑时间、统一写入校验、错误反馈、默认账户规则、转账和余额调整。
2. 尽早补数据安全：迁移测试、本地备份恢复、CSV 导出；在此之前不把应用定位为可长期保存真实财务数据的稳定版。
3. 再补长期使用能力：搜索筛选、分页、CSV 导入、回收站、周期记账。
4. 再做规划与分析：预算、多维报表、日历、多账本。
5. 最后才考虑高权限自动记账、短信识别、悬浮窗和商业化。

不建议直接照搬小星记账的 Activity 数量、广泛权限、商业 SDK 或自动记账方案。应借鉴其领域边界和用户流程，用当前项目的 Compose + Room 架构重新实现。

---

## 2. 分析范围与可信度

### APK 样本

- 文件：`小星记账_4.0.8.apk`
- SHA-256：`C18D86841D455C699C168B85AE8E6E0F2CF31DB317BA3E827DDBEFAA43D16F32`
- 包名：`com.cxincx.xxjz`
- 版本：`4.0.8`，versionCode `4080`
- compileSdk / targetSdk：36；minSdk：23
- 体积：17,027,257 字节
- DEX：2 个，约 12.4 MiB；仅包含 `arm64-v8a` 原生库
- APK v1/v2/v3 签名均验证通过；签名证书 SHA-256：`654dba66...9243803c`

APK 使用百度加固：Application 被替换为 `com.sagittarius.v6.StubApplication`，包含 `libbaiduprotect.so` 和加固载荷。因此无法仅靠静态反编译完整恢复业务实现。本报告对 APK 的结论分为：

- **已确认**：来自清单、资源表、类描述符、原生库、内置帮助文档和 CSV 模板。
- **高可信推断**：由组件命名、页面资源和数据实体组合推导。
- **未验证**：运行时网络协议、数据库字段全集、自动识别算法细节和服务端实现。

### GitHub 仓库

- 仓库：[050701wmy-lang/suiji-android](https://github.com/050701wmy-lang/suiji-android)
- 审阅提交：[`fac36de`](https://github.com/050701wmy-lang/suiji-android/commit/fac36deab611732026068ef332be15dd80958f02)
- 当前只有一个初始化提交，共 35 个文件。

---

## 3. 小星记账 APK 基本框架与技术栈

### 应用框架

小星记账是传统 Android View 架构，而不是 Compose：

- 大量 Activity/Fragment 和 XML layout；清单中约 132 个 Activity、10 个 Service、19 个 Receiver、2 个 Provider。
- AndroidX AppCompat、Fragment、RecyclerView、ViewPager2、ConstraintLayout、Material Components。
- ViewBinding 8.10.1；DEX 中可见 `*Binding` 类。
- AndroidX Lifecycle / ViewModel；业务包中存在多个 `vm`、`ViewModel` 类。
- Kotlin + kotlinx.coroutines 1.6.4。
- WorkManager 2.8.1，用于备份、计划或后台可靠任务。

### 数据与基础设施

- **ObjectBox 是主要业务持久层**：APK 包含 `libobjectbox-jni.so`，并可见 `BoxStore`、`Box`、`Query`、`ToOne`、`ToMany` 和生成的 `Entity_` 类。
- APK 同时携带 Room 2.5.0，但从业务实体和 ObjectBox 生成类看，Room 更可能来自某个依赖或只用于局部功能，不能据此认定它是主账本数据库。
- OkHttp 负责网络访问；未发现明确 Retrofit 特征。
- Glide 负责图片加载，Lottie 负责空状态或引导动画。
- Android FileProvider、WebView、WorkManager、BiometricPrompt。

### 第三方与商业能力

- Bugly Native：崩溃收集。
- 友盟：统计与渠道。
- 微信 OpenSDK、支付宝 SDK：登录、授权或支付。
- 阿里云盘授权、WebDAV/坚果云相关备份入口。
- Google 广告 ID 与 OAID 相关权限/库特征。
- 百度加固：提高逆向门槛。

### 安全与权限画像

它申请了网络、通知、录音、相机、短信、定位、悬浮窗、全部文件访问、无障碍、开机启动、前台服务、指纹等权限。这些权限与自动记账、账单图片、地理位置、短信识别、备份和应用锁相匹配，但也显著增加隐私、合规和应用商店审核成本。

对「随记」而言，这部分只能借鉴“能力分层与明确告知”，不能直接复制权限集合。

---

## 4. 小星记账模块拆解

| 模块 | 静态证据 | 核心能力 |
|---|---|---|
| 启动与主框架 | `StartupActivity`、`MainActivity` | 初始化、激活/登录状态、主导航 |
| 账目 | `DetailCreate/List/Split/Link/Refund/BatchEdit/CalendarActivity` | 新增、编辑、拆分、关联、退款、批量修改、日历查看 |
| 账户 | `AccountDetail/Edit/List/Sort/Chart/ReconciliationActivity` | 多类型账户、分组排序、账户流水、余额图表、对账 |
| 分类 | `TypeManage/Edit/Icon/CommonlyTypeActivity` | 大类/小类、常用分类、图标与排序 |
| 报表 | `ChartList/ChartDetailListActivity`、多种 chart layout | 支出、收入、结余、趋势、分类构成、明细下钻 |
| 预算 | `BudgetActivity/Edit/Detail/TransferActivity` | 分类预算、预算目标、预算调整/转移 |
| 多账本 | `BookManage/Edit/Template/CoverActivity` | 多账本、账本模板、主题/封面 |
| 周期与分期 | `CycleActivity/Edit/List`、`Installment*Activity` | 周期记账、信用卡分期、家庭卡场景 |
| 搜索与筛选 | `SearchActivity`、`SearchFilter*Activity` | 多条件搜索、退款搜索、结果下钻 |
| 导入导出 | `ImportAccount/Detail/ManageActivity`、CSV 模板 | 字段映射、账户匹配、分批导入、导入暂存 |
| 备份恢复 | `BackupService`、`BackupActivity`、`WebDavSettingActivity` | 本地备份、云盘/WebDAV、恢复、后台备份 |
| 自动记账 | AccessibilityService、SMS Receiver、Overlay、AutoDraft | 支付页面/短信识别、草稿、确认、去重、通知 |
| 元数据 | `Tag*`、`Shop*`、`Member*`、`Location*` | 标签、商家、角色/成员、地理位置 |
| 多币种 | `CurrencyManage/EditActivity` | 币种管理及账目币种 |
| 桌面入口 | AppWidget、MIUI Widget、Quick Settings Tile、URL Scheme | 桌面组件、快捷设置、外部唤起记账/搜索 |
| 隐私与商业 | 应用锁、VIP、微信/支付宝支付 | 生物识别、激活、设备绑定、支付 |

内置帮助文档还确认了模板记账、应收应付、报销、AA、理财、借贷、信用卡、退款、预算目标、桌面组件、多设备备份等完整场景。

---

## 5. 小星记账数据层与账务模型

### 已确认的实体线索

DEX 中可见以下 ObjectBox 业务实体：

- `AccountBookEntity`：账本。
- `AccountEntity`：资金账户或借贷对象。
- `DetailEntity`：账目主记录。
- `BudgetItemEntity`：预算项。
- `CycleEntity`：周期任务或分期。
- `ImportBillEntity`：导入暂存记录。
- `LocationEntity`：地理位置。
- `TagEntity`、`TagGroupEntity`：标签与标签组。
- `ConfigEntity`：配置。
- 另有被混淆的实体，结合资源名推断可能覆盖分类、成员、商家等。

### 从导入模板确认的账目字段

CSV 模板字段包括：类型、日期、大类、小类、金额、账户、账户2、报销、备注、图片、角色、标签、币种、商家。

其中“类型”不是简单的收入/支出二选一，而包含：

- 支出 / 收入
- 转账
- 借贷（借入、借出、收款、还款）
- 应付款 / 应收款

这说明成熟记账产品通常不会把所有资金变化都压缩成 `INCOME` 和 `EXPENSE`。转账尤其不能作为一笔支出加一笔收入简单处理，否则会污染收支报表。

### 推荐给随记的目标模型

不要一次复制全部实体。建议逐步演进为：

```text
Ledger
Account
Category
Transaction          # 一次业务事件
TransactionEntry     # 对账户余额的一个方向性影响
Tag / TransactionTag
Attachment
RecurringRule
Budget / BudgetPeriod
ImportBatch / ImportRow
```

短期若不采用完整复式记账，也至少应为 `Transaction` 增加 `kind`：`EXPENSE`、`INCOME`、`TRANSFER`、`REFUND`、`ADJUSTMENT`，并为转账保存 `from_account_id` 与 `to_account_id`。这样以后才能可靠扩展退款、借贷和对账。

---

## 6. 小星记账关键功能流程

### 手动记账

入口（主界面、桌面组件、快捷设置、Scheme） → 选择业务类型 → 金额 → 分类 → 账户/目标账户 → 日期 → 商家、备注、标签、成员、图片、币种等附加信息 → 统一校验 → 写入 `DetailEntity` → 刷新账户余额、明细、预算和报表。

### 转账

选择“账户互转” → 转出账户 + 转入账户 → 金额 → 可选优惠/手续费 → 保存为一个业务事件 → 同时影响两个账户，但不计入普通收入/支出。

### 自动记账

无障碍或短信 Receiver 获取支付结果 → 识别应用、金额、商户、支付账户 → 去重 → 生成 `AutoDraft` → 悬浮窗/通知让用户确认或修改 → 进入统一写入流程。

这里真正值得借鉴的是“自动化只产草稿，最终进入同一写入管线”，这与随记当前 AGENTS 规则一致。

### 导入

选择来源/模板 → 解析到 `ImportBillEntity` 暂存 → 字段校验 → 账户与分类映射 → 展示错误行 → 用户确认 → 批量进入统一账务写入 → 保留批次记录以便撤销或排查。

### 备份恢复

生成包含账本、设置、附件的版本化备份 → 本地或云盘存储 → WorkManager/前台服务保证长任务 → 恢复前校验版本与完整性 → 原子替换或导入 → 重建索引与派生状态。

---

## 7. `suiji-android` 当前架构审阅

### 当前技术栈

由 [版本目录](https://github.com/050701wmy-lang/suiji-android/blob/main/gradle/libs.versions.toml) 和 [app 构建配置](https://github.com/050701wmy-lang/suiji-android/blob/main/app/build.gradle.kts) 可确认：

- Kotlin 2.4.10、Java 17。
- AGP 9.3.1、compile/targetSdk 37、minSdk 33。
- Compose Multiplatform 1.11.1，但当前只有 Android app 模块。
- MIUIX 0.9.3、Navigation 3 1.1.4。
- Room 2.8.4、KSP、Coroutines、Serialization。
- 单 Activity + Compose + 单 app 模块。

### 当前分层

```text
AccountingApplication
  └─ AccountingDatabase / AccountingDao
      └─ AccountingRepository
          └─ AccountingViewModel
              └─ Navigation 3 + Compose Screens
```

这是合适的早期结构，暂时不需要拆成大量 Gradle 模块。

### 已完成的能力

- 账户、分类、账目三张核心表；Room schema 已从 v1 迁移至 v2。
- 金额使用最小货币单位 `Long`，避免浮点误差。
- 账户/分类归档，外键 `RESTRICT` 保留历史关联。
- 手动新增/编辑/删除账目，共用 `TransactionDraft`。
- 默认账户、默认分类与首次启动 seed。
- 账户余额、首页资产负债、账户详情和按月流水。
- 明细按自然日分组、当日/月度摘要。
- 周/月/年报、当前趋势、近六期趋势、分类构成。
- 分类新增及图标持久化。
- 本地规则型自然语言解析，草稿确认后写入。
- 手机/宽屏导航和 MIUIX Blur。

### 当前数据模型

- `AccountEntity`：名称、账户类型、期初余额、排序、默认/归档。
- `CategoryEntity`：名称、收支方向、排序、图标、归档。
- `TransactionEntity`：收入/支出、金额、账户、分类、对象、备注、发生时间、来源。
- DAO 提供全量 Flow，并在界面/ViewModel 中派生余额与报表。

---

## 8. 代码审阅发现

### 当前完成度快照

| 能力 | 状态 | 说明 |
|---|---|---|
| 收支新增、编辑、删除 | 已完成 | 新建与编辑复用同一编辑器，删除有二次确认 |
| 首页资产与账户分组 | 已完成 | 使用 Room 真实账户和账目计算 |
| 账户详情与编辑 | 已完成 | 支持按月流水、预选当前账户记账、默认与隐藏设置 |
| 分类新增 | 已完成 | 分类名称与图标写入 Room，新增后立即选中 |
| 周/月/年报 | 已完成基础版 | 包含周期摘要、趋势、近六期和分类构成 |
| AI 草稿解析 | 部分完成 | 本地解析器和确认页存在，但普通用户入口尚未接通 |
| 历史日期记账 | 未完成 | 时间行尚未真正修改 `occurred_at` |
| 转账、退款、余额调整 | 未完成 | 当前模型只有收入与支出 |
| 备份恢复、导入导出 | 未完成 | 尚不具备长期真实使用的数据安全能力 |
| 搜索、筛选、分页 | 未完成 | 当前仍观察全量账目并在 UI 层聚合 |

### P0：进入真实使用前必须解决

1. **时间看似可编辑，实际不可编辑。** 手动页“时间”行有箭头，但 `occurred_at` 尚不能由用户修改，无法可靠补账或修正历史记录。
2. **统一写入缺少完整领域校验。** Repository 目前主要验证金额，需要同时验证账户、分类、归档状态和收支方向，并把数据库错误转成 UI 可展示结果。
3. **没有转账与余额调整模型。** 当前只能通过修改期初余额或两笔收支模拟，前者丢失审计轨迹，后者污染报表。
4. **没有可验证的备份恢复。** 在提供稳定版前，必须具备迁移测试、版本化本地备份、恢复预检和失败回滚。
5. **默认账户不满足稳定不变量。** 归档默认账户后可能没有有效默认账户，应在同一 Room 事务中选出替代项或阻止不完整状态。

### P1：首个可用版本应解决

1. **AI 能力不可达。** `AiEntryRoute` 和页面已经存在，但主界面没有入口；解析还应优先使用默认账户，并处理空账户、空分类及重复分类异常。
2. **写操作缺少统一状态。** 保存、删除、归档和新增分类需要 loading、成功及可读错误反馈，避免约束异常直接结束协程。
3. **设置仍是隐藏主 Tab。** 应迁移为独立 Navigation 3 二级页，获得明确的返回语义并与主导航解耦。
4. **账户和分类管理仍不完整。** 需要分类编辑、排序、停用恢复；账户当前余额修改最终应改为可追溯的余额调整事件。
5. **系统备份策略未明确。** `android:allowBackup="true"`，但没有显式备份规则、加密边界和用户说明。

### P2：数据规模与体验

1. 所有账目仍被全量观察并在 Compose 层执行 `filter/groupBy/sumOf`；应在搜索与大数据阶段迁移到 DAO 分页和 SQL 聚合。
2. 手动记账缺少对象/商家输入，虽然实体已有对应字段，但新增记录无法填写。
3. minSdk 33 覆盖面较窄，需要明确是产品选择还是 MIUIX/Blur 的技术约束。
4. 当前周期“日均”应明确采用完整周期还是截至今天，避免指标含义不清。
5. 测试目前集中在金额、键盘和 AI 解析，仍缺 Room DAO、v1→v2 迁移、Repository、时区边界、Navigation 与关键 UI 流程测试。

---

## 9. 差距矩阵与借鉴方式

| 领域 | 随记现状 | 小星记账成熟能力 | 建议 |
|---|---|---|---|
| 基础记账 | 收入/支出 CRUD | 转账、借贷、退款、拆分、关联、批量编辑 | 先做转账与退款，拆分/借贷后置 |
| 账户 | 三类账户、期初余额、归档 | 多账户类型、分组、排序、对账、余额图表、提醒 | 加账户分组、排序和调整记录，不直接改历史余额 |
| 分类 | 收支分类、图标、新增 | 大类/小类、常用、排序、完整管理 | 先做编辑、排序、归档/恢复，再考虑两级分类 |
| 明细 | 日期分组 | 搜索、筛选、日历、批量、回收站 | 搜索筛选是高收益功能，应优先 |
| 报表 | 周/月/年基础图表 | 多维度、下钻、账户图表、预算联动 | 将聚合下移 DAO，支持点击分类下钻 |
| 数据安全 | Room migration v2 | 本地/云盘备份、恢复、导入导出 | 首先做本地 JSON/ZIP + CSV，云同步后置 |
| 自动化 | 本地文本解析但入口未接 | 无障碍、短信、草稿、去重、规则配置 | 先接通文本草稿；高权限自动识别最后做 |
| 计划 | 无 | 周期记账、分期、预算目标 | 先周期规则，再预算；分期后置 |
| 多账本 | 未实现，路由预留 | 完整多账本和模板 | 等单账本数据模型稳定后再迁移 |
| 元数据 | 对象/备注字段有限 | 商家、标签、成员、图片、位置、币种 | 优先标签与附件，位置/成员/多币种按需求 |
| 外部入口 | 无 | Widget、Tile、Scheme | 可在核心稳定后做一个简洁 Widget/Shortcut |

---

## 10. 实施架构原则

当前项目继续保持单 `app` 模块和较浅调用链，不为路线图中尚未开发的功能预建大量包、接口或用例类。只有当转账、导入或周期任务实际接入、Repository 出现第二条重复写入流程时，再抽取统一保存协调器。

短期代码边界保持为：

```text
model                 金额、账务类型、草稿和错误
data                   Room Entity、DAO、Migration、Repository
ui                     Navigation 3、ViewModel、MIUIX 页面
ai                     只负责把文本转换为可审阅草稿
```

进入转账阶段后，推荐把一次业务事实与账户资金影响分离：

```text
Transaction            支出、收入、转账、退款、调整等业务事件
TransactionEntry       account_id + signed_amount_minor
```

现有收入或支出迁移为一个 Entry；转账迁移为同一 Transaction 下的一正一负两个 Entry。这样可以同时保证总资产、账户余额、报表排除内部转账和原子编辑删除，不需要用两笔无关联收支模拟。

贯穿所有阶段的约束：

- 手动新增、编辑、AI 草稿、导入和周期任务必须汇入同一条交易保存流程。
- 多账户变化必须在同一个 Room transaction 内完成。
- UI 只维护输入状态和展示结果，不在页面中复制账务校验或写库逻辑。
- 数据库存储可追溯事实；余额、报表和预算都必须能由事实重算。
- 每次 schema 变更必须提交导出 schema、迁移实现、迁移测试和回滚前备份策略。
- 金额始终使用最小货币单位 `Long`；日期区间统一按设备时区转换后查询。
- 不为了模仿参考应用而增加无需求页面、权限、SDK 或 Gradle 模块。

---

## 11. 分阶段开发计划

以下工期按一名开发者的有效开发日估算，不包含等待设计确认、应用商店审核和大范围 UI 返工。每个阶段都应保持应用可构建、可迁移、可安装，不跨阶段堆积未完成数据库变更。

### 阶段 0：当前 UI 与 CRUD 基线（已完成）

范围：三主分页、手动记账、账目编辑删除、分类新增、首页账户分组、账户详情与编辑、基础报表、Room v2 和 MIUIX 页面骨架。

完成标准：`assembleDebug`、单元测试和 Lint 通过；调试包可覆盖安装并冷启动；现有 v1 数据可迁移到 v2。

### 阶段 1：可靠记账与测试安全网（已完成）

目标：让现有收入/支出闭环适合日常真实录入。

- 时间行接入 MIUIX 日期与时间选择器，新建和编辑都能修改 `occurred_at`。
- 增加对象/商家输入，并在明细、账户流水和搜索描述中统一展示。
- Repository 统一校验金额、账户、分类、归档状态和收支方向。
- ViewModel 为保存、删除、归档和新增分类提供明确的进行中、成功和失败状态。
- 默认账户规则改为“存在有效账户时恰好一个默认账户”，新增、切换和归档在事务内处理。
- 增加 Room DAO、Repository、v1→v2 迁移、时区跨日和报表边界测试。
- 明确系统备份策略；在自有恢复能力完成前，至少避免产生未经说明的云端备份行为。

验收标准：

- 可新增、编辑和删除任意历史日期的收支，重启后时间与商家数据不丢失。
- 归档默认账户后会自动得到新的有效默认账户，或在没有账户时保持明确空状态。
- 无效分类方向、归档账户和数据库约束错误会显示可读提示，不闪退、不静默失败。
- 迁移测试和 Repository 测试可在无设备环境运行。

### 阶段 2：AI 入口与基础管理闭环（4～6 天）

目标：把已有但不可达的功能变成完整用户流程。

- 在记账入口中提供 AI 记账选择，不新增第四个主 Tab。
- AI 优先选择默认账户，只生成可审阅草稿；确认后调用与手动记账相同的保存流程。
- 空账户、空分类、无法识别金额和重复分类返回结构化错误。
- 分类支持编辑名称与图标、排序、停用和恢复；被历史账目引用的数据不物理删除。
- 设置迁移为独立 Navigation 3 二级路由，不再借用隐藏主 Tab。
- 为账户与分类增加最小可用的停用项查看入口。

验收标准：AI 草稿的每个字段都可在统一编辑器修改；取消草稿不会写入 Room；手动与 AI 保存产生一致的数据和报表结果。

### 阶段 3：Room v3 与正确的资金事件（7～10 天）

目标：支持转账、退款和可追溯余额调整，不污染收支报表。

- 冻结 `Transaction + TransactionEntry` 数据模型和迁移映射。
- 现有收入/支出迁移为单账户 Entry；增加 `transaction_kind`。
- 转账在同一事件下生成转出和转入 Entry，创建、编辑、删除均使用 Room transaction。
- 退款与原账目建立关联，明确全额退款和部分退款规则。
- 账户余额修改改为 `ADJUSTMENT` 事件，不继续直接改写期初余额；期初余额只用于建账基线。
- 首页净资产、账户详情、明细和报表全部适配新模型。

验收标准：

- A→B 转账后 A 减少、B 增加，总资产不变，普通收入和支出报表不变。
- 编辑或删除转账时两个账户同时回滚，不存在只成功一侧的状态。
- 退款能追溯原账目，调整余额能在账户流水中解释来源。
- v2 真实样本迁移到 v3 后，每个账户余额和原收支报表与迁移前一致。

### 阶段 4：本地备份、恢复与导出（7～10 天）

目标：达到可长期保存真实财务数据的最低安全门槛。

- 使用系统文件选择器创建版本化 ZIP 备份，包含数据库导出、设置、附件清单、格式版本和 SHA-256 校验。
- 恢复前校验格式版本、完整性和可迁移性；恢复失败时保留原数据库。
- 恢复前自动生成一份可回退快照。
- 提供标准 CSV 导出，金额使用十进制文本，时间和时区规则写入导出说明。
- 编写空库恢复、覆盖恢复、损坏文件、旧版本和大数据量测试。

验收标准：新安装应用可从备份恢复出相同账户、分类、账目和余额；损坏文件或未知版本不会覆盖现有数据；备份文件可由用户自主保存和转移。

### 阶段 5：搜索、筛选与数据规模（7～10 天）

目标：让账目量增长后仍可查找和流畅展示。

- 明细改为 Paging 3 或等价的 Room 分页查询。
- DAO 支持日期、业务类型、账户、分类和关键词组合筛选。
- 报表与首页聚合下移 SQL，避免每个页面重复加载和遍历全量账目。
- 增加搜索页、筛选条件回显、账户/分类报表下钻和日历入口。
- 建立 1 万和 10 万条账目的基准数据与结果对照测试。

验收标准：10 万条账目下首页、明细首屏和常用搜索保持可接受响应；分页过程中无重复或漏项；SQL 报表与固定基准数据一致。

### 阶段 6：CSV 导入、回收站与周期记账（10～15 天）

- CSV 导入先写入 `ImportBatch/ImportRow` 暂存，完成字段映射、错误行预览、账户分类匹配和确认提交。
- 导入批次可整体撤销，正式提交仍调用统一交易保存流程。
- 增加账目软删除与回收站，到期清理策略由用户明确触发或配置。
- 周期规则支持频率、下次执行、启停、到期和“生成草稿/自动确认”策略。
- WorkManager 只负责调度，幂等键防止系统重启、时区切换或重复执行造成重复账目。

验收标准：1000 行导入能准确定位错误行并整批撤销；周期规则覆盖月底、闰年、时区变化和系统重启且不重复记账。

### 阶段 7：预算与多账本（10～15 天）

- 先完成月预算和分类预算，金额完全由真实账目重算。
- 新增 `Ledger` 后，为账户、分类、交易、预算和周期规则补充 `ledger_id`，并提供单账本到默认账本的迁移。
- 主界面增加账本切换；现有显式 `account_id` 路由扩展为 `ledger_id + account_id`。
- 约束跨账本账户不能直接转账；如未来需要，另行定义业务规则，不隐式混用数据。

验收标准：升级用户的全部旧数据进入默认账本且结果不变；切换账本后首页、明细、报表、记一笔和默认账户都严格隔离。

### 阶段 8：高级体验与自动记账实验（按需）

- 标签、附件、桌面 Widget、App Shortcut、URL Scheme 和 WebDAV 备份按实际需求选择。
- 自动化优先顺序为分享入口或通知解析、短信识别、最后才是 AccessibilityService 与悬浮窗。
- 所有自动化只创建可审阅草稿，并保存来源、去重标识和解析说明。
- 权限关闭后核心记账必须完全可用，不申请与已启用能力无关的权限。

验收标准：自动化失败不会写错账或重复账；用户能解释、修改、拒绝和追溯每一份自动草稿。

---

## 12. 下一步可直接执行的任务清单

### 迭代 A：可靠记账（已完成）

| ID | 任务 | 依赖 | 完成定义 |
|---|---|---|---|
| A1 | 日期时间编辑 | 无 | 新建/编辑均可选择日期时间，跨日和重启测试通过 |
| A2 | 商家/对象输入 | 无 | 写入现有字段，明细与账户流水展示规则一致 |
| A3 | 统一保存校验 | A1、A2 | 金额、账户、分类、方向和归档状态仅在 Repository 保存入口校验一次 |
| A4 | 写操作状态与错误提示 | A3 | 保存、删除、归档、分类新增均有 loading 和可读失败提示 |
| A5 | 默认账户事务规则 | A3 | 新增、切换、归档后保持有效默认账户不变量 |
| A6 | Room 与 Repository 测试 | A3、A5 | DAO、v1→v2 迁移、时区和失败路径进入自动测试 |
| A7 | 系统备份策略 | 无 | 明确 `allowBackup` 与备份规则，并在设置中说明当前数据保护边界 |

迭代出口：所有测试、Lint 和 Debug 构建通过；覆盖安装现有设备后，旧账目、余额和报表不变。

### 迭代 B：AI 与管理闭环

| ID | 任务 | 依赖 | 完成定义 |
|---|---|---|---|
| B1 | AI 记账入口 | A3、A4 | 从统一记账入口进入，生成草稿而非直接写库 |
| B2 | AI 默认账户与错误模型 | A5、B1 | 优先默认账户，空数据和解析失败可恢复 |
| B3 | 分类编辑与排序 | A4 | 名称、图标、排序、停用、恢复均持久化 |
| B4 | 停用项管理 | B3 | 可查看并恢复账户和分类，历史账目关系不变 |
| B5 | 设置独立路由 | 无 | 设置使用 Navigation 3 二级 entry，返回主页面状态保持 |

迭代出口：手动记账和 AI 草稿共享同一编辑器、校验与保存结果；分类管理不需要直接操作数据库。

### 进入 Room v3 前的设计门槛

在开始转账实现前，必须先确认并记录以下决策：

1. Transaction 与 TransactionEntry 的字段、外键和删除策略。
2. 收入、支出、转账、退款、调整分别如何生成 Entry。
3. 报表是否统计退款净额，以及部分退款的展示方式。
4. v2→v3 迁移样本和迁移后余额不变量。
5. 备份格式如何识别 schema 与业务格式版本。

完成迭代 A、B 后，项目具备可靠扩展账务模型的基础；完成阶段 3 和阶段 4 后，才适合标记为可承载真实个人账本的首个稳定候选版本。

---

## 13. 不建议照搬的部分

- 不要一开始申请短信、定位、录音、相机、悬浮窗、无障碍和全部文件访问。
- 不要把百度加固、友盟、Bugly、支付 SDK 当作产品成熟度的必要条件。
- 不要用 Activity 数量衡量模块化；Compose 应以状态、路由和领域边界组织。
- 不要直接复制 UI、图标、文案、资源或反编译代码；只借鉴交互模式和领域设计。
- 不要在转账、退款、导入、周期任务中分别写数据库逻辑。
- 不要在没有可靠备份恢复前鼓励用户长期存放真实财务数据。

最终方向应是：保留随记现在清晰、轻量、本地优先、MIUIX 风格的优势，吸收小星记账在账务类型、账户管理、数据迁移、搜索报表和长期自动化方面的产品经验。
